import React from "react";

type PageProps = {
    id: number,
  title: string;
  description: string;
  genre: string;
  duration: number;
  poster_url: string;
  release_date: string;
};

const MovieInner: React.FC<PageProps> = ({}) => {

return(<div>
    
</div>);

}

export default MovieInner;